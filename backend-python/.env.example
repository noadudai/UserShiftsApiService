# backend-python environment variables
# The Python FastAPI service currently has no required environment variables.
# Add any future configuration here (e.g. database connection, external API keys).
